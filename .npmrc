# Host packages are peers on purpose: in a profile they resolve to the single
# copy the DSH installation anchor links in. Never let a package manager install
# a second copy of a cordis Service class.
auto-install-peers=false
strict-peer-dependencies=false
